# Data

`processed/study_level_data.csv` is generated by `run_analysis.R` from the archived Excel workbook. It adds explicit provenance, imputation, eligibility, and analysis flags while preserving the selected-study values.

Do not edit the generated CSV manually. Make transparent corrections in `R/01_prepare_data.R`, rerun the pipeline, and document the rationale in the project README.

